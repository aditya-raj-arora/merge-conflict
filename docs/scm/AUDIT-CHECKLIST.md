# Audit Checklist

This is the checklist we actually run through at every baseline promotion —
i.e., every time we're about to cut a `BL-<NN>-<name>` tag. The whole point
of a Configuration Audit is catching drift between "what got approved" and
"what's actually about to ship," so this isn't a formality, it's the last
gate before something becomes a baseline.

Copy this checklist into the PR/Issue for the release (or just tick it off
here and note the baseline tag it was run for) before tagging.

## Checklist

- [ ] **CodeQL clean** — no unresolved high/critical findings on the branch being promoted
- [ ] **gitleaks clean** — no secrets flagged in the full-history scan
- [ ] **Coverage delta ≥ 0** — Codecov patch check isn't red, overall project coverage hasn't dropped (as of CR-089: uploads are real and reports are live at
      [codecov.io](https://app.codecov.io/github/aditya-raj-arora/merge-conflict), but no `codecov/patch`/`codecov/project` GitHub status check has posted yet on any commit, and they are deliberately **not** required in branch protection until one does - see that CR for why. Until then, this check means "look at the actual Codecov report by hand," not "trust a green check")
- [ ] **Dependabot security queue empty** — no open, unmerged security-advisory PRs
- [ ] **All CRs listed in the release are resolved** — every `CR-NNN` referenced in the CHANGELOG entries for this release is actually closed
- [ ] **Tag signature verifies** — `git tag -v <tag>` shows a good signature
- [ ] **Baseline register entry drafted** — the new row for `BASELINE-REGISTER.md` is ready (tag, date, signer, included CIs, closed CRs, approver, notes)
- [ ] **Status ledger updated** — affected rows in `STATUS-LEDGER.md` reflect the new version/environment

## Why each check exists

| Check             | What it catches                                                                                   |
| ----------------- | ------------------------------------------------------------------------------------------------- |
| CodeQL            | A known-bad code pattern slipping into an "approved" baseline                                     |
| gitleaks          | A secret getting baked permanently into git history                                               |
| Coverage delta    | Untested code quietly becoming part of the shipped configuration                                  |
| Dependabot queue  | A known CVE in a dependency being present in what we're about to call "approved"                  |
| CRs resolved      | A baseline claiming to include work that was never actually finished/reviewed                     |
| Tag signature     | No way to prove _who_ approved this baseline, or that it wasn't tampered with after the fact      |
| Baseline register | The whole point of an audit — if this table's stale, nobody downstream can trust "what's in prod" |
| Status ledger     | Same idea, but for "where is it," not "what is it"                                                |

## Audit log

| Baseline tag                                      | Date       | Run by           | Result                                      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------- | ---------- | ---------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BL-01-identification-intro`                      | 2026-08-06 | Aditya Raj Arora | Pass, with one gap noted                    | CodeQL: clean (0 open alerts). gitleaks: clean. Coverage delta: **not measured** - Codecov was never connected to a real account/token, so `codecov/patch` isn't a real signal yet (dropped from required checks back in Step 5). Dependabot queue: empty. CRs resolved: all 8 closed as part of this audit (CR-001, CR-003, CR-019, CR-021, CR-024, CR-027, CR-029, CR-032) - they'd been merged for a while but never auto-closed, since PRs used `Refs:` instead of a GitHub closing keyword; fixed the PR template (see CR-038) so this doesn't recur. Tag signature: verifies (`git tag -v`). Register + ledger: this PR.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `BL-02-dependency-hardening`                      | 2026-08-20 | Aditya Raj Arora | Pass, with two gaps noted                   | CodeQL: clean (0 open alerts, both `code-scanning/alerts` and per-PR checks). gitleaks: clean on every PR. Coverage delta: **still not measured** - same Codecov gap as BL-01, unchanged. Dependabot queue: empty (6 PRs merged this cycle: #49, #46, #45, #44, #43, #41). CRs resolved: **none formally opened** - this baseline is routine dependency/CI hygiene (Dependabot bumps + the deploy-pipeline fixes already closed under BL-01's CRs), no user-facing or content change, so no `CR-NNN` was filed; noted as a process gap in `BASELINE-REGISTER.md` rather than silently skipped. Tag signature: verifies (`git tag -v v0.1.1` and `git tag -v BL-02-dependency-hardening`, both good). Register + ledger: this PR. One real bug caught mid-audit: Dependabot's vite 6→8 PR (#41) broke `npm ci` (ERESOLVE against `@vitejs/plugin-react@4.7.0`) and had been sitting red for 10 days - fixed by bumping `@vitejs/plugin-react` to `^6.1.0` in the same PR, verified locally (typecheck/lint/test/build all green) before merge. Prod deploy independently verified live via the required-reviewer gate (run `32366836650`, approved and completed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `BL-03-version-control-intro`                     | 2026-08-20 | Aditya Raj Arora | Pass, with two gaps noted                   | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: **still not measured** - same Codecov gap, unchanged since BL-01. Dependabot queue: empty. CRs resolved: CR-051 (Chapter 2 level) and CR-054 (release-please MINOR-bump fix) both closed via their PRs' closing keywords. Tag signature: verifies (`git tag -v v0.2.0` and `git tag -v BL-03-version-control-intro`, both good). Register + ledger: this PR. **Gap 1 (known, not a defect):** `LVL-02-01-whose-fix-made-it` is shipped and tested but not wired into `App.tsx` - no chapter/level-select UI exists, so it's not reachable from the live site yet; verified playable by swapping the import locally and inspecting the rendered SVG instead of via the deployed build. **Gap 2 (process, resolved during this audit):** two separate GitHub Actions push-webhook misses this cycle - one on the CR-054 fix landing on `develop` (no workflow fired at all for commit `6f262f4`; confirmed via githubstatus.com this wasn't a broader outage, fixed via a follow-up empty-commit PR #56), and one structural gap where release-please's own bot-authored (`GITHUB_TOKEN`) push to PR #53 never triggered its `pull_request` checks at all (expected GitHub behavior to prevent workflow recursion, not a bug - worked around with a human-authored empty commit on that branch). Prod deploy independently verified live via the required-reviewer gate (run `32372789573`, approved and completed).                                                                                                                                                                                                                                                                                                                                                                |
| `BL-04-level-select-ui`                           | 2026-08-20 | Aditya Raj Arora | Pass, gap closed                            | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: **still not measured** - Codecov gap unchanged. Dependabot queue: empty. CRs resolved: CR-058 (level-select UI) and CR-061 (release policy clarification) both closed via their PRs. Tag signature: verifies (`git tag -v v0.2.1` and `git tag -v BL-04-level-select-ui`, both good). Register + ledger: this PR. **BL-03's Gap 1 is now closed:** `LVL-02-01-whose-fix-made-it` is reachable from the live site - independently confirmed by loading the real prod URL post-deploy, not just the local dev server. **New process learning:** CR-061's first `Release-As: 0.2.1` attempt silently failed because GitHub's squash-merge drops the source commit body (and any trailer) unless `--body`/`--body-file` is passed explicitly - release-please's own run log confirmed it never even parsed a `Release-As` directive (`PR ... remained the same`), which is how PR #60 stayed at the wrong `v0.3.0` for one full cycle before the retry (PR #63, merged with `--body-file`) fixed it. Also hit the now-familiar bot-authored-push gap on release-please's own branch again (worked around the same way as BL-03). Prod deploy independently verified live via the required-reviewer gate (run `32377340364`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `BL-05-change-control-intro`                      | 2026-08-20 | Aditya Raj Arora | Pass, no gaps                               | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: **still not measured** - Codecov gap unchanged. Dependabot queue: empty. CRs resolved: CR-065 (Chapter 3 level) closed via its PR. Tag signature: verifies (`git tag -v v0.3.0` and `git tag -v BL-05-change-control-intro`, both good). Register + ledger: this PR. First baseline since BL-01 with no known gap to carry forward - the level was registered in `content/levelManifest.ts` in the same PR that added it, so there was no shipped-but-unreachable window like BL-03 had. release-please correctly proposed `v0.3.0` (MINOR) unprompted - no `Release-As` override needed, confirming BL-04's policy clarification works as intended for an unambiguous new-chapter case. Prod deploy independently verified live via the required-reviewer gate (run `32383196063`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ directly - all three chapters now listed on the live level-select screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `BL-06-status-accounting-intro`                   | 2026-08-20 | Aditya Raj Arora | Pass, no gaps                               | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: **still not measured** - Codecov gap unchanged. Dependabot queue: empty. CRs resolved: CR-069 (Chapter 4 level) closed via its PR. Tag signature: verifies (`git tag -v v0.4.0` and `git tag -v BL-06-status-accounting-intro`, both good). Register + ledger: this PR. No gaps to carry forward, same as BL-05 - level registered in the manifest in the same PR that added it. First level to deliberately depart from Ch1-3's "spot the missing/wrong signal in a static graph" puzzle shape (raised as a concern at the end of BL-05): this one is about a claim (status page text) diverging from ground truth (prod's actual ref), still inside the existing schema with zero engine changes. release-please correctly proposed `v0.4.0` (MINOR) unprompted. Prod deploy independently verified live via the required-reviewer gate (run `32385107323`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ directly - all four chapters now listed on the live level-select screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `BL-07-configuration-audit-intro`                 | 2026-08-20 | Aditya Raj Arora | Pass, no gaps                               | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: **still not measured** - Codecov gap unchanged. Dependabot queue: empty. CRs resolved: CR-073 (Chapter 5 level) closed via its PR. Tag signature: verifies (`git tag -v v0.5.0` and `git tag -v BL-07-configuration-audit-intro`, both good). Register + ledger: this PR. No gaps to carry forward. Self-referential level: `LVL-05-01-does-it-still-match` asks the player to do almost exactly what this very audit-log row is proof of doing - checking that what's live still matches the last signed baseline. release-please correctly proposed `v0.5.0` (MINOR) unprompted. Prod deploy independently verified live via the required-reviewer gate (run `32387892326`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ directly - all five chapters now listed on the live level-select screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `BL-08-build-release-intro`                       | 2026-08-20 | Aditya Raj Arora | Pass, no gaps - final chapter               | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: **still not measured** - Codecov gap unchanged since BL-01, never resolved across all 8 baselines. Dependabot queue: empty. CRs resolved: CR-083 (Chapter 6 level) closed via its PR. Tag signature: verifies (`git tag -v v0.6.0` and `git tag -v BL-08-build-release-intro`, both good). Register + ledger: this PR. No gaps to carry forward. **All six SCM disciplines now have a first playable slice, live on prod** - Configuration Identification (BL-01), Version Control (BL-03), Change Control (BL-05), Status Accounting (BL-06), Configuration Audit (BL-07), Build & Release Management (BL-08). Self-referential level again: `LVL-06-01-which-tag-lied` is the exact class of versioning-policy bug (commit type vs. semver bump mismatch) this repo's own release-please pipeline hit for real via CR-054 and CR-061. Prod deploy independently verified live via the required-reviewer gate (run `33485506775`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ directly - all six chapters now listed on the live level-select screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `CR-089` (no baseline tag - infra, not a release) | 2026-09-01 | Aditya Raj Arora | Partial fix, gap narrowed                   | Not a baseline promotion - a standalone fix to the recurring Coverage delta gap flagged in every prior audit-log entry. `CODECOV_TOKEN` added as a repo secret; verified real by inspecting two separate CI run logs directly ("Your upload is now queued for processing...") and by Codecov posting its first-time welcome comment on the PR - not just assumed from a green check. **Still open:** no `codecov/patch`/`codecov/project` GitHub status check has posted on either verified commit despite the upload succeeding both times. Briefly added both as required branch-protection checks on `develop`/`main`, then reverted within the same session after confirming neither had posted on two commits - a required check that never reports would have permanently blocked every future merge. Deferred fixing the underlying Codecov-side setting (likely a "GitHub Status Checks" toggle) to the repo owner's own dashboard access, at their explicit choice, rather than leaving it silently broken or guessing at a fix I couldn't verify.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `BL-09-branching-story-mechanic`                  | 2026-09-01 | Aditya Raj Arora | Pass, first content supersession            | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: real now (Codecov connected as of CR-089) but no blocking status check yet - same deferred state noted there. Dependabot queue: empty. CRs resolved: CR-091 closed via its PR. Tag signature: verifies (`git tag -v v0.7.0` and `git tag -v BL-09-branching-story-mechanic`, both good). Register + ledger: this PR. **First time this project has retired previously-baselined content:** `LVL-01-01-which-one-shipped` (baselined at BL-01) is replaced by `STORY-01-01-which-one-shipped`. Handled per this repo's own rule - BL-01's row is untouched, not edited retroactively; a supersession note was appended to it instead, pointing here. New `Story` engine mechanic is additive: `Level`, `useGameStore`, `LevelView`, and all 5 quiz-format levels (Ch2-6) verified unaffected (regression-checked via the full test suite, 58/58 passing). Prod deploy independently verified live via the required-reviewer gate (run `33492286375`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ and playing through the new branching story directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `BL-10-vn-ui-deeper-branches`                     | 2026-09-01 | Aditya Raj Arora | Pass, no gaps                               | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: real but still not a blocking check (unchanged since CR-089). Dependabot queue: empty. CRs resolved: CR-095 closed via its PR. Tag signature: verifies (`git tag -v v0.8.0` and `git tag -v BL-10-vn-ui-deeper-branches`, both good). Register + ledger: this PR. Visual-novel `StoryView` rebuild (speaker plate, typewriter reveal, mood backdrop, gated choice menu) and `STORY-01-01` deepened to 5-7 stages on its consequence paths, with a genuine second decision on the build-a side producing two distinct bad endings. Schema additions (`speaker`/`mood`/`autoNext`) verified additive - full regression suite (68/68) confirms `Level`/`useGameStore`/`LevelView` and all 5 quiz-format levels unaffected. Prod deploy independently verified live via the required-reviewer gate (run `33494829592`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ and confirming the VN backdrop/speaker plate render correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `BL-11-ch02-vn-story`                             | 2026-09-01 | Aditya Raj Arora | Pass, no gaps - second content supersession | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: real but still not a blocking check (unchanged since CR-089). Dependabot queue: empty. CRs resolved: CR-099 closed via its PR. Tag signature: verifies (`git tag -v v0.8.1` and `git tag -v BL-11-ch02-vn-story`, both good). Register + ledger: this PR. Second content supersession (first was BL-09) - `LVL-02-01-whose-fix-made-it` (baselined at BL-03) replaced by `STORY-02-01-whose-fix-made-it`; BL-03's row untouched, supersession note appended instead. Correctly classified as PATCH before merging, not accepted as release-please's default MINOR - this redesigns existing content into an already-shipped mechanic, not a new chapter/mechanic (same reasoning as BL-04). Verified the `Release-As: 0.8.1` trailer actually survived the squash-merge by checking the merged commit directly, rather than assuming and finding out later the way BL-04's first attempt silently failed. Prod deploy independently verified live via the required-reviewer gate (run `33496987360`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ and confirming Chapter 2's story loads correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `BL-12-ch03-vn-story`                             | 2026-09-01 | Aditya Raj Arora | Pass, no gaps - third content supersession  | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: real but still not a blocking check (unchanged since CR-089). Dependabot queue: empty. CRs resolved: CR-105 closed via its PR. Tag signature: verifies (`git tag -v v0.8.2` and `git tag -v BL-12-ch03-vn-story`, both good). Register + ledger: this PR. Third content supersession (BL-09, BL-11, now BL-12) - `LVL-03-01-who-skipped-review` (baselined at BL-05) replaced by `STORY-03-01-who-skipped-review`; BL-05's row untouched, supersession note appended. Correctly classified as PATCH before merging, consistent with BL-11's precedent. **New process instance:** the push landing CR-105's `Release-As: 0.8.2` fix hit a genuine GitHub Actions webhook delivery miss - confirmed via the GitHub API that no check-runs or statuses posted at all for that commit, despite the branch genuinely updating. Same class of gap as BL-03's process note, not a repeat of BL-04's squash-merge-drops-the-trailer bug (the trailer itself was verified intact by inspecting the merged commit directly, same discipline as BL-11). Resolved with a second human-authored empty commit on the release-please branch. Prod deploy independently verified live via the required-reviewer gate (run `33525131361`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ and confirming Chapter 3's story loads correctly.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `BL-13-ch04-vn-story`                             | 2026-09-01 | Aditya Raj Arora | Pass, no gaps - fourth content supersession | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: real but still not a blocking check (unchanged since CR-089). Dependabot queue: **not empty** - #103 (`lint-staged` 16.4.0→17.4.1) sitting open, routine and unrelated to this baseline, not addressed as part of it. CRs resolved: CR-106 closed via its PR. Tag signature: verifies (`git tag -v v0.8.3` and `git tag -v BL-13-ch04-vn-story`, both good). Register + ledger: this PR. Fourth content supersession (BL-09, BL-11, BL-12, now BL-13) - `LVL-04-01-whats-actually-live` (baselined at BL-06) replaced by `STORY-04-01-what-does-prod-actually-run`; BL-06's row untouched, supersession note appended. Correctly classified as PATCH before merging, consistent with BL-11/BL-12's precedent. **New process instance - double webhook-delivery miss:** hit the recurring gap twice in one release cycle. First on release-please's own PR branch (bot-authored push, resolved with one human-authored retrigger commit, consistent with BL-03/BL-04's precedent). Second, novel this time, on the actual push to `develop` carrying the release-please squash-merge itself - zero check-runs or statuses at all (confirmed via the GitHub API), and because `develop` is branch-protected, a direct empty-commit push (the prior fix pattern) was rejected outright; resolved instead with a small dedicated retrigger PR (#113) merged through the normal required-checks gate. `Release-As: 0.8.3` trailer verified intact on the squash-merged commit before waiting on release-please, same discipline as BL-11/BL-12. Prod deploy independently verified live via the required-reviewer gate (run `33531012688`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ and confirming Chapter 4's story loads correctly. |
| `BL-14-ch05-vn-story`                             | 2026-09-01 | Aditya Raj Arora | Pass, no gaps - fifth content supersession  | CodeQL: clean (0 open alerts). gitleaks: clean on every PR. Coverage delta: real but still not a blocking check (unchanged since CR-089). Dependabot queue: **not empty** - #103 (`lint-staged` 16.4.0→17.4.1) still sitting open, routine and unrelated to this baseline. CRs resolved: CR-107 closed via its PR. Tag signature: verifies (`git tag -v v0.8.4` and `git tag -v BL-14-ch05-vn-story`, both good). Register + ledger: this PR. Fifth content supersession (BL-09, BL-11, BL-12, BL-13, now BL-14) - `LVL-05-01-does-it-still-match` (baselined at BL-07) replaced by `STORY-05-01-does-it-still-match`; BL-07's row untouched, supersession note appended. Correctly classified as PATCH before merging, consistent with BL-11 through BL-13's precedent. **No process gap this cycle:** release-please's own PR branch hit the usual bot-authored-push gap (resolved with one human-authored retrigger commit, as always), but the `develop`-side push for the squash-merge itself fired checks normally on the first try - confirmed via the GitHub API - the first release in this VN-redesign sequence not to need a develop-side retrigger, unlike BL-13. `Release-As: 0.8.4` trailer verified intact on the squash-merged commit before waiting on release-please, same discipline as BL-11 through BL-13. Prod deploy independently verified live via the required-reviewer gate (run `33536476862`, approved and completed) and by loading https://aditya-raj-arora.github.io/merge-conflict/ and confirming Chapter 5's story loads correctly.                                                                                                                                                                                                                                                                                        |
