# Copy this file to .env and fill in real values. .env itself is gitignored
# and must never be committed - this file only holds placeholders so people
# know what variables exist.

# Example (not currently used, placeholder for future chapters that might
# need an API key for e.g. a hosted leaderboard):
# VITE_API_KEY=your_api_key_here
